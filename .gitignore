/target
*.db
auto
dist/
.claude/
# metrics/*.json IS committed (trend visible in git history).
# Only ignore raw nextest scratch + tmp tree paths.
/tmp/

bilimanager

# Local-only scratch (QA screenshots from doqa, ad-hoc HTML copies). Never committed.
# Use docs/ for committed artifacts.
.docs/
.DS_Store
